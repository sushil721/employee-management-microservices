# Getting Started with Create React App
```bash
npx create-react-app employee-management-app
cd employee-management-app
npm start
```

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

### install bootstrap
```bash
npm install bootstrap --save
```

## import bootstrap css in index.js 
```bash
import 'bootstrap/dist/css/bootstrap.min.css'
```
### install AXIOS for api-gateway call.
```bash
npm install axios --save
```

### Add Extension tool for creating class sinippet.
*react code snippets
*Create component file EmployeeComponent.js
*Inside EmployeeComponent.js Type -> rcc and press enter. It is create component class.

